* public - 公开属性；默认修饰符
* private - 私有属性；属性只可以当前类中使用（不能被子类继承）
* protected - 可以在当前类 、子类中使用 （可以被子类继承）

```javascript
class Person {
    private name: string;
    protected age: number;    
		private gender: string;

    constructor(name: string, age: number) {
        this._name = name;
        this._age = age;
    }

    getName() {
        return this._name;
    }

    setName(value) {
        this._name = value;
    }

    getAge() {
        return this._age;
    }

    setAge(value) {
        if(value >= 0){
            this._age = value;
        }
    }
}

const person = new Person('pang', 10);
person.gender = 'male'; //报错， private不能在class外部直接修改
person.setAge(20);

class B extends Person {
  test() {
    console.log(this.name);
    console.log(this.age);
    console.log(this.gender); // Property 'gender' is private and only accessible within class 'Person'.
  }
}
```
#### 存取器
```javascript
class Person {
  private _name:string;
	private _age: number;

  constructor(name: string, age: number) {
    this._name = name;
    this._age = age;
  }

  //getter 方法用来获取属性
  //setter 方法用来设置属性
  //称为属性存取器
  get name(){
    return this._name;
  }

  set name(value: string){
    this._name = value;
  }

  get age() {
    return this._age;
  }

  set age(value) {
    if(value >= 0) {
      this._age = value;
    }
  }
}

const person = new Person('p', 10);

person.age = 20;
console.log(person);
console.log(person.name);
```

#### constructor() 语法糖
```javascript
class A {
  // constructor 这种方式传参， 相当于赋值 
  //this.name = name;this.age = age;
  constructor(public name: string, public age: number) {

  }
}

const a = new A('a', 10);
console.log(a);
```

