// 1. 类型声明
let x: string;

// 1）typescript中没有赋值使用变量报错
// console.log(x);

// 2. 类型推断
let foo = 1;

// 类型推断foo为number，赋值为string会报错
// foo = 'string'

// 3. 编译
// Typescript -> Javascript
// Typescript 制作人编译时的类型检查，而不是运行时的检查

// 6. tsc 编译器
